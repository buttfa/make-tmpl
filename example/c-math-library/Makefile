ARGS=LIB_NAME=libcmath.a  LIB_TYPE=static HEADER_FOLDERS='inc inc/calc' SOURCE_ROOT_FOLDER= SOURCE_FOLDERS='src/calc'

lib:
	make $(ARGS) -f ../../Makefile

info:
	make info $(ARGS) -f ../../Makefile

clean:
	make clean $(ARGS) -f ../../Makefile